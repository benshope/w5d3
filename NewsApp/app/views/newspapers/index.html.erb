<h1>Newspapers</h1>
<ul>
	<% @newspapers.each do |newspaper| %>
		<li><%= link_to newspaper.name, newspaper_path(newspaper) %></li>
	<% end %>
</ul>
<%= link_to "Create New Newspaper", new_newspaper_path, class: "btn" %>